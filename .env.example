# Backend base URL. The default in src/lib/api.js points at a temporary
# pinggy.net dev tunnel that WILL expire — copy this file to .env and set
# the real URL once the backend has a stable address.
VITE_API_BASE_URL=https://your-backend.example.com
